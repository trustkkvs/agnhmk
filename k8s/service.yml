apiVersion: v1
kind: Service
metadata:
  name: afhfhg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "afhfhg"
    port: 8080
  selector:
    app: afhfhg
